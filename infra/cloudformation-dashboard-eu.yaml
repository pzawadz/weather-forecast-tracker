AWSTemplateFormatVersion: '2010-09-09'
Description: 'Weather Dashboard - CloudFront + ALB + EC2 Streamlit (EU-WEST-1)'

Parameters:
  InstanceId:
    Type: String
    Default: i-0acff8ad77effe38c
    Description: EC2 instance ID (Ireland app server)
  
  VpcId:
    Type: AWS::EC2::VPC::Id
    Default: vpc-01cfa6fef167dc90c
    Description: VPC ID
  
  SubnetIds:
    Type: CommaDelimitedList
    Description: Subnet IDs for ALB (must include eu-west-1c where EC2 is located)
  
  DashboardPort:
    Type: Number
    Default: 8502
    Description: Port where Streamlit dashboard runs (must match systemd service)

Resources:
  # Security Group for ALB
  ALBSecurityGroup:
    Type: AWS::EC2::SecurityGroup
    Properties:
      GroupDescription: Allow HTTP from CloudFront only
      VpcId: !Ref VpcId
      SecurityGroupIngress:
        - IpProtocol: tcp
          FromPort: 80
          ToPort: 80
          CidrIp: 0.0.0.0/0
          Description: HTTP from anywhere (CloudFront validates via secret header)
      SecurityGroupEgress:
        - IpProtocol: tcp
          FromPort: !Ref DashboardPort
          ToPort: !Ref DashboardPort
          CidrIp: 172.31.0.0/16
          Description: To Streamlit on EC2
      Tags:
        - Key: Name
          Value: weather-dashboard-alb-sg

  # Application Load Balancer
  ApplicationLoadBalancer:
    Type: AWS::ElasticLoadBalancingV2::LoadBalancer
    Properties:
      Name: weather-dashboard-alb
      Type: application
      Scheme: internet-facing
      IpAddressType: ipv4
      Subnets: !Ref SubnetIds
      SecurityGroups:
        - !Ref ALBSecurityGroup
      Tags:
        - Key: Name
          Value: weather-dashboard-alb

  # Target Group
  TargetGroup:
    Type: AWS::ElasticLoadBalancingV2::TargetGroup
    Properties:
      Name: weather-dashboard-tg
      Port: !Ref DashboardPort
      Protocol: HTTP
      VpcId: !Ref VpcId
      TargetType: instance
      HealthCheckEnabled: true
      # Streamlit native health check endpoint - lightweight, returns "ok"
      HealthCheckPath: /_stcore/health
      HealthCheckProtocol: HTTP
      HealthCheckIntervalSeconds: 30
      HealthCheckTimeoutSeconds: 10
      HealthyThresholdCount: 2
      UnhealthyThresholdCount: 3
      Matcher:
        HttpCode: 200
      Targets:
        - Id: !Ref InstanceId
          Port: !Ref DashboardPort
      Tags:
        - Key: Name
          Value: weather-dashboard-tg

  # ALB Listener with secret header validation
  ALBListener:
    Type: AWS::ElasticLoadBalancingV2::Listener
    Properties:
      LoadBalancerArn: !Ref ApplicationLoadBalancer
      Port: 80
      Protocol: HTTP
      DefaultActions:
        - Type: fixed-response
          FixedResponseConfig:
            StatusCode: 403
            ContentType: text/plain
            MessageBody: Direct access not allowed

  # Listener Rule with secret header
  ListenerRule:
    Type: AWS::ElasticLoadBalancingV2::ListenerRule
    Properties:
      ListenerArn: !Ref ALBListener
      Priority: 1
      Conditions:
        - Field: http-header
          HttpHeaderConfig:
            HttpHeaderName: X-CloudFront-Secret
            Values:
              - !Sub '{{resolve:secretsmanager:weather-dashboard-cf-secret-eu:SecretString:secret}}'
      Actions:
        - Type: forward
          TargetGroupArn: !Ref TargetGroup

  # CloudFront Distribution
  CloudFrontDistribution:
    Type: AWS::CloudFront::Distribution
    Properties:
      DistributionConfig:
        Enabled: true
        Comment: Weather Dashboard (EU-WEST-1)
        DefaultCacheBehavior:
          TargetOriginId: alb-origin
          ViewerProtocolPolicy: redirect-to-https
          AllowedMethods:
            - GET
            - HEAD
            - OPTIONS
            - PUT
            - POST
            - PATCH
            - DELETE
          CachedMethods:
            - GET
            - HEAD
          Compress: true
          ForwardedValues:
            QueryString: true
            Headers:
              - Host
              - Origin
              - Referer
              - User-Agent
              - CloudFront-Forwarded-Proto
              - CloudFront-Is-Desktop-Viewer
              - CloudFront-Is-Mobile-Viewer
              - CloudFront-Is-SmartTV-Viewer
              - CloudFront-Is-Tablet-Viewer
            Cookies:
              Forward: all
          MinTTL: 0
          DefaultTTL: 0
          MaxTTL: 86400
        Origins:
          - Id: alb-origin
            DomainName: !GetAtt ApplicationLoadBalancer.DNSName
            CustomOriginConfig:
              HTTPPort: 80
              OriginProtocolPolicy: http-only
              OriginReadTimeout: 60
              OriginKeepaliveTimeout: 5
            OriginCustomHeaders:
              - HeaderName: X-CloudFront-Secret
                HeaderValue: !Sub '{{resolve:secretsmanager:weather-dashboard-cf-secret-eu:SecretString:secret}}'
        PriceClass: PriceClass_100
        ViewerCertificate:
          CloudFrontDefaultCertificate: true
        HttpVersion: http2

Outputs:
  CloudFrontURL:
    Description: CloudFront Distribution URL
    Value: !Sub 'https://${CloudFrontDistribution.DomainName}'
    Export:
      Name: WeatherDashboardEUURL

  ALBDNSName:
    Description: Application Load Balancer DNS Name
    Value: !GetAtt ApplicationLoadBalancer.DNSName
    Export:
      Name: WeatherDashboardEUALB

  ALBSecurityGroupId:
    Description: ALB Security Group ID (add to EC2 inbound rules for port 8502)
    Value: !Ref ALBSecurityGroup
    Export:
      Name: WeatherDashboardEUALBSG

  TargetGroupArn:
    Description: Target Group ARN
    Value: !Ref TargetGroup
    Export:
      Name: WeatherDashboardEUTG
